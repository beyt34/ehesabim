@using eHesabim.Framework.Localization
@using Kendo.Mvc.UI
@model eHesabim.Web.Portal.Models.CustomerTransactionListWebModel

<div class="row">
    <div class="col-lg-12">
        &nbsp;
    </div>
</div>

<div class="panel panel-yellow">

    <div class="panel-heading">
        <h3 class="panel-title">
            <i class="fa fa-edit fa-fw"></i> @Headers.CustomerTransactionList
        </h3>
    </div>

    <div class="panel-body">
        @using (Html.BeginForm(null, null, FormMethod.Post, new { id = "customerTransactionForm", @class = "form-horizontal" })) {
            <div class="form-group">
                <label class="col-sm-2">@Html.Raw(Labels.Customer)</label>
                <div class="col-sm-10">
                    @Html.DropDownListFor(m => m.SearchCustomerId, Model.SearchCustomerList, new { @class = "form-control", placeholder = Labels.Customer })
                </div>
            </div>

            <div class="form-group">
                <label class="col-sm-2">@Labels.Description</label>
                <div class="col-sm-10">
                    @Html.TextBoxFor(m => m.SearchName, new { @class = "form-control", placeholder = Labels.Description })
                </div>
            </div>

            <div class="form-group">
                <label class="col-sm-2">@Html.Raw(Labels.StartDate)</label>
                <div class="col-sm-10">
                    @Html.Kendo().DatePickerFor(m => m.SearchStartDate).HtmlAttributes(new { type = "text" })
                </div>
            </div>

            <div class="form-group">
                <label class="col-sm-2">@Html.Raw(Labels.EndDate)</label>
                <div class="col-sm-10">
                    @Html.Kendo().DatePickerFor(m => m.SearchEndDate).HtmlAttributes(new { type = "text" })
                </div>
            </div>

            <div class="form-group">
                <label class="col-sm-2">@Labels.CustomerExclusion</label>
                <div class="col-sm-10">
                    @Html.DropDownListFor(m => m.SearchExcludeId, Model.SearchExcludeList, new {@class = "form-control"})
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-2">&nbsp;</div>
                <div class="col-sm-10">
                    <a href="#" id="form-search" class="UIButton btn btn-warning">@Buttons.Search</a>
                    <a href="#" id="form-detail" class="btn btn-outline btn-warning">@Buttons.Add</a>
                </div>
            </div>
        }

        <hr />

        <div class="table-responsive">
            @(Html.Kendo().Grid(Model.Data)
                  .Name("customerTransactionGrid")
                  .Columns(columns => {
                      columns.Bound(x => x.CustomerName).Title(Labels.Customer);
                      columns.Bound(x => x.TrnDateTime).Format("{0:dd.MM.yyyy}").Title(Labels.Date);
                      columns.Bound(x => x.Name).Title(Labels.Description).HtmlAttributes(new { style = "white-space: nowrap; max-width: 400px; overflow: hidden;" });
                      columns.Bound(x => x.Debit).Format("{0:N2}").HtmlAttributes(new { style = "text-align:right;" }).Title(Labels.Debit).Sortable(false);
                      columns.Bound(x => x.Claim).Format("{0:N2}").HtmlAttributes(new { style = "text-align:right;" }).Title(Labels.Claim).Sortable(false);
                      columns.Bound(x => x.DueDateTime).Format("{0:dd.MM.yyyy}").Title(Labels.DueDate).Sortable(false);
                      columns.Bound(x => x.Installment).HtmlAttributes(new { style = "text-align:right;" }).Title(Labels.Installment).Sortable(false);
                      columns.Command(x => x.Custom(Buttons.Edit).Click("onCustomerTransactionEditWindow")).HtmlAttributes(new { cmd = "edit", style = "text-align:center;" });
                      columns.Command(x => x.Custom(Buttons.Delete).Click("onDeleteWindow")).HtmlAttributes(new { cmd = "delete", style = "text-align:center;" });
                  })
                  .Pageable(p => p.Refresh(true).PreviousNext(true))
                  .Sortable()
                  .EnableCustomBinding(true)
                  .Events(events => events.DataBound("onDataBound"))
                  .DataSource(dataSource => dataSource
                    .Ajax()
                    .Model(model => model.Id(p => p.Id))
                    .PageSize(20)
                    .Total(Model.Total)
                    .Read(read => read.Action("CustomerTransactionList", "Customer").Data("GetSearchModel"))
                  )
            )
        </div>

        <hr />

        <!-- Alt Toplamlar -->
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <tr>
                    <th class="col-xs-4">
                        <label>@Labels.DebitTotal</label>
                    </th>
                    <td class="col-xs-8">
                        <div class="input-group">
                            <input type="text" id="debitTotal" class="form-control" style="text-align: right;" readonly="readonly" />
                            <span class="input-group-addon">TL</span>
                        </div>
                    </td>
                </tr>
                <tr>
                    <th>
                        <label>@Labels.ClaimTotal</label>
                    </th>
                    <td>
                        <div class="input-group">
                            <input type="text" id="claimTotal" class="form-control" style="text-align: right;" readonly="readonly" />
                            <span class="input-group-addon">TL</span>
                        </div>
                    </td>
                </tr>
                <tr>
                    <th>
                        <label>@Labels.NetTotal</label>
                    </th>
                    <td>
                        <div class="input-group">
                            <input type="text" id="netTotal" class="form-control" style="text-align: right;" readonly="readonly" />
                            <span class="input-group-addon">TL</span>
                        </div>
                    </td>
                </tr>
            </table>
        </div>

    </div>
</div>

@{Html.Kendo().Window()
          .Name("customerTransactionEditWindow")
          .Title(Headers.CustomerTransactionEdit)
          .Draggable(true)
          .Modal(true)
          .Visible(false)
          .Render();
}

@Html.Partial("_DeleteWindow", Model.DeleteData)

@section scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            DefaultButton("customerTransactionForm", "form-search");

            $('#form-search').click(function () {
                FormSearch(GetSearchModel(true), "customerTransactionGrid");
            });

            $('#form-detail').click(function () {
                var wnd = $("#customerTransactionEditWindow").kendoWindow({
                    content: {
                        url: "/Customer/_CustomerTransactionEdit",
                        data: { customerId: '' + $('#SearchCustomerId').val() + '' }
                    }
                }).data("kendoWindow");

                wnd.wrapper.addClass("col-sm-8 col-xs-10 max-width-650");
                wnd.center();
                wnd.open();
            });
        });

        SetLeftMenu("liCustomer");

        function onCustomerTransactionEditWindow(e) {
            e.preventDefault();
            var dataItem = this.dataItem($(e.currentTarget).closest("tr"));
            openModal("customerTransactionEditWindow", "/Customer/_CustomerTransactionEdit", dataItem.Id, "md");
        }

        function GetSearchModel(searchViaForm) {
            return {
                SearchCustomerId: $('#SearchCustomerId').val(),
                SearchName: $('#SearchName').val(),
                SearchStartDate: $('#SearchStartDate').val(),
                SearchEndDate: $('#SearchEndDate').val(),
                SearchExcludeId: $('#SearchExcludeId').val(),
                SearchViaForm: searchViaForm == undefined ? false : searchViaForm
            };
        }

        function onDataBound(e) {
            if (e.sender._data[0] !== undefined) {
                var debitTotal = e.sender._data[0].DebitTotal;
                var claimTotal = e.sender._data[0].ClaimTotal;
                var netTotal = e.sender._data[0].NetTotal;

                $('#debitTotal').val(debitTotal);
                $('#claimTotal').val(claimTotal);
                $('#netTotal').val(netTotal);
            } else {
                $('#debitTotal').val("0,00");
                $('#claimTotal').val("0,00");
                $('#netTotal').val("0,00");
            }

            onGridDataBound();
        }
    </script>
}
